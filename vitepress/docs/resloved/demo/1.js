// 可以跳1-2，跳10步
const slution = () => {
	const res = [];
	const path = [];
	const dfs = (startIndex) => {
		if (path.length === 10) {
			res.push(path);
			return;
		}
		for (let i = startIndex; i <= 2; i++) { 
			
		}
	}
}